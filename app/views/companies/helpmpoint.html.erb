<%= render "welcome/help_top0" %>

<nav class="navbar">
  <ul class="nav nav-pills pull-left">
    <li><%= link_to "Назад", if @company.nil? then companies_path(:id => @flr.id, :page => @page) else company_path(@company.id, :flr_id => @flr.id) end, :class => "glyphicon glyphicon-level-up" %></li>
</nav>

<%= render "welcome/help_top2" %>

<%= render "welcome/mpoint" %>