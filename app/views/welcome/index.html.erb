<%= link_to "Выход", destroy_user_session_path, method: :delete %>
<center>
	<%= render "mpoints/messages" %>   
<% if current_user.has_role? :cduser %>
<%= link_to "Добавить потребителя", :controller => "companys", 
            :action => "new" %>
            <h1>Список поставщиков</h1> 
<table class="table table-striped">
		<thead>
			<tr>
			   <th>№</th>
			   <th>Наименование</th>
			   <th>Действие</th>
			</tr>	
		</thead>
		<tbody>
					<% i = 0%>
					<% @furns.each do |fr| %>
					<tr>
						<td><%= i = i + 1 %></td>
						<td><%= fr.name %></td>						
						<td><%= link_to "Открыть", furnizor_path(fr) %></td>			
					</tr>		
					<% end %>			
		</tbody>
</table>
  
<% end %>
<% if current_user.has_role? :setsu %>
<!--  <%= render "setsuindex" %>  -->
<% end %>
